{{#if this.showCard}}
<AuCard @flex={{true}} @divided={{true}} @expandable={{false}} @shadow={{true}} @size="small" as |c|>
  <c.header>
    <AuHeading @level="3" @skin="6">
      Mobiliteitsmaatregel
    </AuHeading>
  </c.header>
  <c.content>
    <AuButton {{on "click" (fn this.toggleModal true)}} @icon="plus-text" @iconAlignment="left" @skin="link-secondary">
      Voeg zonaal mobiliteitsmaatregel in
    </AuButton>

    <AuButton {{on "click" (fn this.toggleModal false)}} @icon="plus-text" @iconAlignment="left" @skin="link-secondary">
      Voeg niet zonaal mobiliteitsmaatregel in
    </AuButton>
  </c.content>
</AuCard>

<RoadsignsModal @modalOpen={{this.modalOpen}} @isZonal={{this.isZonal}} @closeModal={{this.toggleModal}} @controller={{@controller}} @insert={{this.insert}}/>
{{/if}}